git init .->cria um repo git na pasta atual
git statis->mostra o status do trabalho(de cada arquivo)
git add nome_arquivo->adiciona arquivo para ser commit
git commit -m "msg"->faz commit do que estava para ser

